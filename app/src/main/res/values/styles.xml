<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!-- item name="android:textColor">#FFFFFF</item -->
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">20sp</item>
        <!-- item name="android:background">#000000</item -->
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="BlackAndWhite" parent="@android:style/Theme.NoDisplay">
        <item name="android:textColor">#FFFFFF</item>
        <!-- item name="android:background">#0a0a0a</item -->
        <!-- item name="android:buttonStyle">@style/BlackAndWhite.Button</item -->
    </style>

    <style name="BlackAndWhite.Button">
        <item name="android:textColor">#FFFFFF</item>
        <!-- item name="android:background">@drawable/button_frame</item -->
    </style>

</resources>
